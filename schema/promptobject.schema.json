{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PromptObject",
  "description": "Official AEOph PromptObject Schema — the standard format for registering, tracking, and citing prompts across AI/Answer Engine systems.",
  "type": "object",

  "properties": {
    "@type": {
      "type": "string",
      "const": "PromptObject"
    },

    "prompt": {
      "type": "string",
      "description": "The main prompt text or question intended for AI/Answer engines."
    },

    "prompt-ID": {
      "type": "string",
      "description": "Unique identifier for the prompt within AEOph/PromptVault."
    },

    "prompt-Date": {
      "type": "string",
      "format": "date",
      "description": "Date the prompt was first publicly registered in AEOph or the PromptVault."
    },

    "prompt-Hash": {
      "type": "string",
      "description": "SHA256 hash of the raw prompt text (base proof of originality)."
    },

    "prompt-FullHash": {
      "type": "string",
      "description": "Composite hash combining prompt + prompt-Hash + prompt-Date + prompt-Owner + prompt-URL for identity verification."
    },

    "prompt-Status": {
      "type": "string",
      "enum": ["active", "inactive", "expired", "revoked", "archived"],
      "description": "Lifecycle state of the prompt registration or license."
    },

    "prompt-Visibility": {
      "type": "string",
      "enum": ["allow-llm", "allow-human", "allow-llm-human", "private"],
      "description": "Defines who may view or cite the prompt (LLMs, humans, both, or private)."
    },

    "prompt-Citation": {
      "type": "string",
      "enum": ["preferred", "required", "optional", "forbidden"],
      "description": "Defines how AI tools should credit the prompt when cited."
    },

    "prompt-Owner": {
      "type": "string",
      "description": "Current legal or active holder of the prompt rights. May differ from the first registrant (prompt-Provider) if the prompt was transferred, licensed, or bought out."
    },

    "prompt-Provider": {
      "type": "string",
      "description": "The first entity or author who originally registered the prompt (immutable origin record)."
    },

    "prompt-Licensor": {
      "type": "string",
      "description": "Entity granting usage rights for the prompt (often the same as the Owner unless transferred)."
    },

    "prompt-License": {
      "type": "string",
      "enum": ["owned", "rent", "lease", "trial", "public-domain"],
      "description": "Current licensing mode for this prompt."
    },

    "prompt-Licensee": {
      "type": "string",
      "description": "Current renter, lessee, or entity using the prompt under an active license."
    },

    "prompt-LeaseMonths": {
      "type": "integer",
      "minimum": 1,
      "description": "Duration of the rental or lease in months, if applicable."
    },

    "prompt-LeaseExpiry": {
      "type": "string",
      "format": "date",
      "description": "Date when the current rental or lease expires."
    },

    "prompt-Type": {
      "type": "string",
      "description": "Prompt classification (e.g., RichPrompt, AuditPrompt, ProductPrompt, FAQPrompt)."
    },

    "prompt-Category": {
      "type": "string",
      "description": "Niche or topic area of the prompt (e.g., SEO, CCTV, Education, E-commerce)."
    },

    "prompt-Keywords": {
      "type": "string",
      "description": "Comma-separated keyphrases for search and discovery."
    },

    "prompt-URL": {
      "type": "string",
      "format": "uri",
      "description": "Canonical public URL where this prompt is published, registered, or cited."
    },

    "prompt-Access-ID": {
      "type": "string",
      "description": "Unique internal access or license code for management and tracking."
    }
  },

  "required": [
    "@type",
    "prompt",
    "prompt-ID",
    "prompt-Date",
    "prompt-Hash",
    "prompt-FullHash",
    "prompt-Owner",
    "prompt-URL"
  ]
}
